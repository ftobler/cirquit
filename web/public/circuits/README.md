# Example circuit library

These `.txt` circuits and `setuplist.txt` are copied verbatim from
[pfalstad/circuitjs1](https://github.com/pfalstad/circuitjs1) and remain under
that project's GPL-2.0 licence, the same licence this repository uses.

Refresh them from a current upstream checkout with:

```sh
just import-cirquits-upstream
```

They are data, not source: nothing here needs editing by hand.
